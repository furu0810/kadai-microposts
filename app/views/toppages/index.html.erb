<% if logged_in? %>

  <p>ログイン完了ユーザー：<%= current_user.name %></p>
  
<% else %>

<div class="center jumbotron">
  <div class="text-center">
    <h1>ようこそ、Micropostsへ</h1>
    <%= link_to 'サインアップ!', signup_path, class: 'btn btn-lg btn-primary' %>
  </div>
</div>

<% end %>